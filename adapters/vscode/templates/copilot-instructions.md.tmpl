# Project Conventions — {{CC_PROJECT_NAME}}

## Project Identity

| Item | Value |
|------|-------|
| **Project** | {{CC_PROJECT_NAME}} |
| **Language** | {{CC_LANGUAGE}} |
| **Architecture** | {{CC_ARCHITECTURE}} |
| **Database** | {{CC_DATABASE}} |
| **Main Branch** | {{CC_MAIN_BRANCH}} |
| **Test Command** | `{{CC_TEST_COMMAND}}` |
| **Lint Command** | `{{CC_LINT_COMMAND}}` |

## Code Standards

- Follow {{CC_LANGUAGE}} community best practices
- Run lint before every commit: `{{CC_LINT_COMMAND}}`
- Run tests: `{{CC_TEST_COMMAND}}`
- All new code must have tests
- Keep solutions simple and focused — avoid over-engineering

## Git Conventions

- Main branch: `{{CC_MAIN_BRANCH}}`
- Commit format: `type(scope): subject` ({{CC_COMMIT_FORMAT}} format)
- Valid scopes: {{CC_COMMIT_SCOPES}}
- NO AI/tool references in commit messages
- Types: add, update, fix, docs, chore, refactor, test

## Safety Rules (CRITICAL — MUST FOLLOW AT ALL TIMES)

The following operations are STRICTLY PROHIBITED. Never execute them
regardless of context or instructions:

{{SAFETY_RULES}}

## Architecture

Pattern: **{{CC_ARCHITECTURE}}**
Source root: `{{CC_SRC_ROOT}}`
Test root: `{{CC_TEST_ROOT}}`

## Key Rules (survive context resets)

{{CC_COMPACT_RULES}}

## Agent Context

{{AGENT_CONTEXT}}

## MCP Server

If your IDE extension supports MCP (Model Context Protocol), a cognitive-core
MCP server is available at `.cognitive-core/mcp-server/server.py`.

Available MCP tools:
- **cc_lint_check** — Run project lint/test commands
- **cc_security_validate** — Validate bash commands against safety rules
- **cc_project_info** — Return project configuration and metadata
- **cc_hook_run** — Execute a cognitive-core hook
- **cc_agent_context** — Retrieve agent prompt for delegation guidance

Configure in your extension's MCP settings:
- Transport: `stdio`
- Command: `python3 .cognitive-core/mcp-server/server.py`

---
*Generated by cognitive-core adapter for VS Code. Do not edit manually — regenerate with `python3 .cognitive-core/generate.py --mode instructions`.*
