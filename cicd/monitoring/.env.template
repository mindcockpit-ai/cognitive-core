# =============================================================================
# Environment Variables Template — cognitive-core monitoring stack
# =============================================================================
# Finding #15 fix: ALL credentials externalized from compose/config files.
#
# Usage:
#   cp .env.template .env
#   # Edit .env with real values
#   # NEVER commit .env to version control
# =============================================================================

# ---------------------------------------------------------------------------
# Grafana
# ---------------------------------------------------------------------------
# Admin user for Grafana web UI
GRAFANA_ADMIN_USER=admin

# REQUIRED: Set a strong password (Finding #15 — no hardcoded defaults)
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_strong_password_here

# Public URL for Grafana (used in alert links)
GRAFANA_ROOT_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Slack Notifications
# ---------------------------------------------------------------------------
# Incoming webhook URL for Slack alerts
# Create at: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/TXXXXX/BXXXXX/XXXXXXXXXXXXXXXX

# Channel overrides (defaults shown)
SLACK_CHANNEL_CRITICAL=#alerts-critical
SLACK_CHANNEL_WARNINGS=#alerts-warnings
SLACK_CHANNEL_CICD=#cicd-alerts

# ---------------------------------------------------------------------------
# Email / SMTP
# ---------------------------------------------------------------------------
# SMTP server for email alerts
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_FROM=alerts@example.com
SMTP_USERNAME=alerts@example.com
SMTP_PASSWORD=CHANGE_ME_smtp_password

# Recipient for alert emails
ALERT_EMAIL_TO=team@example.com

# ---------------------------------------------------------------------------
# PagerDuty
# ---------------------------------------------------------------------------
# Service integration key (Events API v2)
# Create at: https://support.pagerduty.com/docs/services-and-integrations
PAGERDUTY_SERVICE_KEY=CHANGE_ME_pagerduty_key

# ---------------------------------------------------------------------------
# Alertmanager
# ---------------------------------------------------------------------------
# Public URL for Alertmanager (used in silence links)
ALERTMANAGER_URL=http://localhost:9093

# ---------------------------------------------------------------------------
# GitHub Actions Runner
# ---------------------------------------------------------------------------
# Organization and repository for self-hosted runner
GITHUB_ORG=your-org
GITHUB_REPO=your-repo

# Runner registration token
# Generate at: https://github.com/ORG/REPO/settings/actions/runners/new
RUNNER_TOKEN=CHANGE_ME_runner_token

# Runner naming
RUNNER_NAME_PREFIX=runner
RUNNER_LABELS=self-hosted,linux,x64
RUNNER_GROUP=default
RUNNER_SCOPE=repo
RUNNER_EPHEMERAL=false

# ---------------------------------------------------------------------------
# Docker Socket Access (Finding #6)
# ---------------------------------------------------------------------------
# Docker group ID on the host — required for Docker-in-Docker in runners.
# Find with: getent group docker | cut -d: -f3
DOCKER_GID=999

# ---------------------------------------------------------------------------
# Nginx / Reverse Proxy
# ---------------------------------------------------------------------------
# External port for the monitoring Nginx proxy
NGINX_PORT=8888

# ---------------------------------------------------------------------------
# Pushgateway
# ---------------------------------------------------------------------------
# URL for CI scripts to push metrics (Finding #16 — localhost only)
PUSHGATEWAY_URL=http://127.0.0.1:9091

# ---------------------------------------------------------------------------
# Project Identity
# ---------------------------------------------------------------------------
# Used as metric label and container prefix
PROJECT_NAME=myproject
