---
paths: ["**/*.cs"]
---

# C# Conventions

- Use nullable reference types (`#nullable enable`)
- Prefer primary constructors and records for data types
- Async methods return `Task`/`ValueTask` — suffix with `Async`
- Use dependency injection via constructor parameters
