# Session 2026-04-17 → 2026-04-18

Two calendar days, one continuous session. Focus: security hardening around `version.json` RCE surface + workflow robustness + board hygiene.

## Completed

### PR mindcockpit-ai/cognitive-core#259 — merged (closes #255)
**Skill-sync preamble fix + workflow defensive guards**

- Root cause: `core/skills/skill-sync/SKILL.md:25` preamble used brittle `grep|sed` JSON parse + trailing `git log --oneline -3 2>/dev/null` that made the `if`-block return non-zero on any git failure. Harness treated any stderr+nonzero as hard failure. `/skill-sync` unusable.
- Fix in SKILL.md (3 sites): replaced `grep|sed` with `command -v jq && jq -r '.source // ""' || grep|sed` two-tier pattern matching `core/utilities/check-update.sh:27`. Appended `|| true` to the preamble `git log`.
- New suite `tests/suites/22-skill-sync-preamble.sh` — 22 assertions. Covers: valid JSON, whitespace variants, missing file, absent source key, bad path, uninitialized git repo, jq-absent fallback (shadow PATH trick), security allowlist guard (rejects `curl|wget|nc|ssh|eval|/dev/tcp|bash -i|python -c|perl -e` in extracted preamble before `bash -c`).
- Workflow hardening: 7 GraphQL mutation sites in `.github/workflows/project-board-automation.yml` wrapped with `|| { warn; exit 0|continue; }` — single-issue jobs use `exit 0`, loop jobs use `continue`. Consistent `::warning::Board mutation failed for #N` text.
- Peer review: 4 scopes returned GO-WITH-CHANGES. All blocking findings addressed in v2 of branch (R1 conformance `.source // ""`; S4 allowlist guard). Fork-PR RCE path verified not exploitable (suite 22 not in ci.yml fork-PR matrix).
- Branch: `fix/255-skill-sync-and-board-automation-guards` — merged as `01cd92f9`.

### PROJECT_PAT rotation — classic with read:org
**Root cause of `issue-opened` workflow failure on 2026-04-16/17**

Prior fine-grained PAT could not resolve org-owned ProjectV2 node `PVT_kwDODLDmb84BPbGR` in GraphQL mutations. `board-reconcile` scheduled job appeared green but had 0 items to mutate, so never exercised the failing path. Every `issue-opened` event failed hard.

- New PAT: classic, scopes `repo`+`project`+`read:org`, expires **2027-06-21**
- Storage: macOS Keychain (`github-project-pat`, `pewo`), VPS pass (`github/project-pat` at mindcockpit.ai), GH repo secret `PROJECT_PAT`
- dev-notes/docs/token-matrix.md committed as `a0b89a8` (private repo)
- Smoke-tested with issue #258 (now closed, Done on board)
- Calendar reminder: **2027-06-14** — rotate before expiry

### Issue rescope — mindcockpit-ai/cognitive-core#256 split into #256 + #260

Original #256 ("validate `$SOURCE` before exec") went through 4-scope peer review. Solution-architect returned STOP: hardcoded default `$HOME/workspace/cognitive-core` wrong; only 2 consumer sites listed but 6+ actually affected; trust-anchor infrastructure missing.

Rescoped into:

- **mindcockpit-ai/cognitive-core#260** (new, P1, size:S) — trust-anchor prerequisite:
  - `install.sh` writes `CC_FRAMEWORK_ROOT=<resolved SCRIPT_DIR>` into conf at install time
  - TOFU migration for existing installs (pin from `version.json.source` once, idempotent)
  - `cognitive-core.conf` hardened to 0444 + owner-check (`CC_CONF_OWNER_SKIP=1` escape hatch for CI/Docker)
  - Inline migration in `setup-env.sh` with atomic `mkdir` lock (session-guard.sh pattern)
  - Regression suite 24 with ≥20 assertions
  - 4-scope peer review complete, final reviewed prompt at `docs/sessions/2026-04-18-260-implementation-prompt.md`
  - Implementation plan drafted (5 phases, ~4.5h estimated)

- **mindcockpit-ai/cognitive-core#256** (rewritten, P1, size:M, `blocked` by #260) — actual mitigation:
  - `_cc_validate_framework_source` helper in `core/hooks/_lib.sh`
  - Export `CC_VALIDATED_SOURCE`; callers MUST use that, not raw `$SOURCE`
  - Apply at **9 consumer sites** (not 2): skill-sync SKILL.md ×3, check-update.sh, health-check.sh, skill-updater.md, setup-env.sh, update.sh, plugin/ mirrors
  - Validate immediately before each exec (TOCTOU mitigation)
  - Attack vectors: null bytes, `..`, SUID/setgid, nested symlinks, `update.sh`-as-symlink, CC_FRAMEWORK_ROOT unset/empty
  - Suite 23 min 25 assertions; wrong-owner test `_skip` when not root

### Triage — 23 issues labeled

All 100 open issues now uniformly tagged (priority + area + kind).
- 6 Group A (priority + area + kind): #240, #238, #237, #236, #235, #234
- 17 Group B (kind only): 11 enhancement, 5 documentation, 1 bug

## Artifacts

- Branch `fix/255-skill-sync-and-board-automation-guards` (merged as 01cd92f9)
- Session doc: `docs/sessions/2026-04-18-session.md` (this file)
- #260 reviewed implementation prompt: `docs/sessions/2026-04-18-260-implementation-prompt.md`

## Pending for Next Session

### Immediate — mindcockpit-ai/cognitive-core#260 implementation (size:S, ~4.5h)
Prompt is reviewed and ready. Steps:
1. Move #260 to In Progress on board (triggers auto-branch if enabled, or create `fix/260-framework-root-anchor` manually off main)
2. Phase 1 — install.sh: `CC_FRAMEWORK_ROOT` in conf heredoc, chmod 0644→write→0444, owner check with stat fallback chain + `CC_CONF_OWNER_SKIP=1` honor
3. Phase 2 — setup-env.sh: inline TOFU migration (no helper — Parsimony), atomic `mkdir` lock at `.claude/cognitive-core/anchor.lock.d`, anchored `grep -qE '^CC_FRAMEWORK_ROOT='` detect
4. Phase 3 — `tests/suites/24-framework-root-anchor.sh`: ≥20 assertions per matrix in prompt. Seed test_dir with suite 04 pattern; source `_lib.sh` directly
5. Phase 4 — `tests/run-all.sh`: pretty-name
6. Phase 5 — verify: full test run, `bash -n` clean, fresh install + pre-upgrade synthetic smoke, `install.sh --force` works against 0444, `update.sh` sources cleanly

### Then — mindcockpit-ai/cognitive-core#256 unblocks
Validator helper + apply at 9 consumer sites + ≥25-assertion suite 23. Estimated size:M.

### Other open items
- **Pre-existing WIP in working tree** (not mine, untouched all session): `adapters/_adapter-lib.sh`, `adapters/claude/adapter.sh`, `install.sh`, `tests/suites/09-adapter-interface.sh` — decide commit/stash/discard next session
- **Calendar**: add 2027-06-14 reminder "Rotate PROJECT_PAT"
- Decision: post the #260 reviewed prompt as an issue comment? (currently only in-repo at `docs/sessions/2026-04-18-260-implementation-prompt.md`)

## Key facts to preserve across restart

| Item | Value |
|------|-------|
| Branch merged | `fix/255-skill-sync-and-board-automation-guards` → main (01cd92f9) |
| Main up-to-date | Yes — local main fast-forwarded post-merge |
| Pre-existing WIP | adapters/, install.sh, tests/suites/09 (unresolved, not from this session) |
| New issues filed | mindcockpit-ai/cognitive-core#256 (rescoped), #260 (new), #258 (test, closed) |
| PAT secret | Rotated 2026-04-17T13:14:48Z; expires 2027-06-21 |
| New suites on main | 22 (skill-sync-preamble) |
| Test run status | Suites 1-22 unrelated to WIP all pass; 9 suites fail due to pre-existing WIP (not this session's responsibility) |

## Related sessions

- Previous: `docs/sessions/2026-04-14-session.md` (#154 orphan subprocess + #240 Phase 1 hook hardening)
- Related docs: `docs/research/inter-session-coordination.md`
